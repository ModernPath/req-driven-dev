interface:
  display_name: "RDD Build"
  short_description: "Build or correct an approved SR slice"
  default_prompt: "Use $rdd-build to execute the next approved SR slice or in-scope review correction."
